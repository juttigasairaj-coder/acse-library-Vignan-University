import { createACSE LibraryConfig } from "ACSE Library-agent-playwright-config/config";

export default createACSE LibraryConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
